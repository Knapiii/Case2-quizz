<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="case2.iths.com.QuizGame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/AppName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LayoutExamples.LayoutExampleActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".StatisticActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".HandleQuestionsActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".PlayGameActivity" />
        <activity android:name=".LayoutExamples.MoonActivity"></activity>
    </application>

</manifest>